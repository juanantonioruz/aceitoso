<%=form_for [@museo,@info] do |f|%>

    <%=f.label :tipo,:class=>'label'%>
    <%= f.select(:tipo_ri, dameTiposRI()) %>

    <%=f.label :articulo,:class=>'label'%>
    <%=f.collection_select(:generica_id, Generica.all, :id,:titulo,:prompt => 'Elija la información')%>
    <br/>
    <%=f.submit 'Guardar',:class=>'btn btn-success'%> o 
	<%=link_to 'cancelar',museo_infos_path(@museo)%>
<%end%>
