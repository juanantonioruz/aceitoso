module InfosHelper
    def titulo(url)
      generica=Generica.find(url)
      return generica.titulo.to_s
    end
    def url(info)
      generica=Generica.find(info.url)
      return link_to(generica.titulo,generica_path(generica))
    end
end
